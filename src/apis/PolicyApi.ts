/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type AffectedPoliciesResponse,
    AffectedPoliciesResponseFromJSON,
    AffectedPoliciesResponseToJSON,
} from '../models/AffectedPoliciesResponse.js';
import {
    type AgentPolicyCounts,
    AgentPolicyCountsFromJSON,
    AgentPolicyCountsToJSON,
} from '../models/AgentPolicyCounts.js';
import {
    type CreatePolicy,
    CreatePolicyFromJSON,
    CreatePolicyToJSON,
} from '../models/CreatePolicy.js';
import {
    type FilterCategory,
    FilterCategoryFromJSON,
    FilterCategoryToJSON,
} from '../models/FilterCategory.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type PaginationResponsePolicyImpactSummary,
    PaginationResponsePolicyImpactSummaryFromJSON,
    PaginationResponsePolicyImpactSummaryToJSON,
} from '../models/PaginationResponsePolicyImpactSummary.js';
import {
    type PaginationResponsePolicyRevisionSummary,
    PaginationResponsePolicyRevisionSummaryFromJSON,
    PaginationResponsePolicyRevisionSummaryToJSON,
} from '../models/PaginationResponsePolicyRevisionSummary.js';
import {
    type PaginationResponsePolicySummary,
    PaginationResponsePolicySummaryFromJSON,
    PaginationResponsePolicySummaryToJSON,
} from '../models/PaginationResponsePolicySummary.js';
import {
    type PolicyDetailResponse,
    PolicyDetailResponseFromJSON,
    PolicyDetailResponseToJSON,
} from '../models/PolicyDetailResponse.js';
import {
    type PolicyImpactSeverityCounts,
    PolicyImpactSeverityCountsFromJSON,
    PolicyImpactSeverityCountsToJSON,
} from '../models/PolicyImpactSeverityCounts.js';
import {
    type PolicyImpactSummary,
    PolicyImpactSummaryFromJSON,
    PolicyImpactSummaryToJSON,
} from '../models/PolicyImpactSummary.js';
import {
    type PolicyRevisionCompareResponse,
    PolicyRevisionCompareResponseFromJSON,
    PolicyRevisionCompareResponseToJSON,
} from '../models/PolicyRevisionCompareResponse.js';
import {
    type PolicyRevisionDetailResponse,
    PolicyRevisionDetailResponseFromJSON,
    PolicyRevisionDetailResponseToJSON,
} from '../models/PolicyRevisionDetailResponse.js';
import {
    type PolicyRuleInput,
    PolicyRuleInputFromJSON,
    PolicyRuleInputToJSON,
} from '../models/PolicyRuleInput.js';
import {
    type PolicySummaryResponse,
    PolicySummaryResponseFromJSON,
    PolicySummaryResponseToJSON,
} from '../models/PolicySummaryResponse.js';
import {
    type UpdatePolicy,
    UpdatePolicyFromJSON,
    UpdatePolicyToJSON,
} from '../models/UpdatePolicy.js';
import {
    type ValidatePolicyRequest,
    ValidatePolicyRequestFromJSON,
    ValidatePolicyRequestToJSON,
} from '../models/ValidatePolicyRequest.js';
import {
    type ValidatePolicyResponse,
    ValidatePolicyResponseFromJSON,
    ValidatePolicyResponseToJSON,
} from '../models/ValidatePolicyResponse.js';

export interface AcknowledgePolicyImpactRequest {
    /**
     * 
     */
    policyId: string;
    /**
     * 
     */
    impactId: string;
}

export interface ClonePolicyRequest {
    /**
     * 
     */
    policyId: string;
}

export interface ComparePolicyRevisionsRequest {
    /**
     * 
     */
    policyId: string;
    /**
     * 
     */
    from: string;
    /**
     * 
     */
    to: string;
}

export interface CreatePolicyRequest {
    /**
     * 
     */
    createPolicy: CreatePolicy;
}

export interface GetPolicyRequest {
    /**
     * 
     */
    policyId: string;
}

export interface GetPolicyRevisionRequest {
    /**
     * 
     */
    policyId: string;
    /**
     * 
     */
    revisionId: string;
}

export interface ListAllPolicyImpactsRequest {
    /**
     * Filter by status. Default: open, acknowledged.
     */
    status?: Array<string> | null;
    /**
     * Filter by severity
     */
    severity?: Array<string> | null;
    /**
     * Filter to one policy
     */
    policyId?: string | null;
    /**
     * Filter to one MCP server
     */
    mcpServerId?: string | null;
    /**
     * Filter to one catalog snapshot
     */
    snapshotId?: string | null;
    /**
     * Page number (1-based)
     */
    page?: number;
    /**
     * Number of items per page (max 100)
     */
    limit?: number;
}

export interface ListPoliciesRequest {
    /**
     * Search by policy ID, name, description, or support contact
     */
    search?: string | null;
    /**
     * Filter by status values
     */
    status?: Array<string> | null;
    /**
     * Filter by MCP server IDs
     */
    mcpServerId?: Array<string> | null;
    /**
     * Filter by agent/application IDs
     */
    agentId?: Array<string> | null;
    /**
     * Column to sort by
     */
    sortBy?: ListPoliciesSortByEnum;
    /**
     * Sort direction
     */
    sortOrder?: ListPoliciesSortOrderEnum;
    /**
     * Filter to non-archived policies with at least one open or acknowledged policy impact (true), or the complement (false)
     */
    hasImpacts?: boolean | null;
    /**
     * Page number (1-based)
     */
    page?: number;
    /**
     * Number of items per page (max 100)
     */
    limit?: number;
}

export interface ListPoliciesAffectedByRequest {
    /**
     * Agent (application) UUID. Mutually exclusive with mcp_server_id.
     */
    agentId?: string | null;
    /**
     * MCP server UUID. Mutually exclusive with agent_id.
     */
    mcpServerId?: string | null;
}

export interface ListPolicyImpactsRequest {
    /**
     * 
     */
    policyId: string;
    /**
     * Filter by status. Unset: every status (history view).
     */
    status?: Array<string> | null;
}

export interface ListPolicyRevisionsRequest {
    /**
     * 
     */
    policyId: string;
    /**
     * Free-text across author name and summary.
     */
    q?: string | null;
    /**
     * Multi-value OR filter.
     */
    category?: Array<string> | null;
    /**
     * Page number (1-based)
     */
    page?: number;
    /**
     * Number of items per page (max 100)
     */
    limit?: number;
}

export interface UpdatePolicyRequest {
    /**
     * 
     */
    policyId: string;
    /**
     * 
     */
    updatePolicy: UpdatePolicy;
}

export interface ValidatePolicyOperationRequest {
    /**
     * 
     */
    validatePolicyRequest: ValidatePolicyRequest;
}

export interface ValidatePolicyRuleRequest {
    /**
     * 
     */
    policyRuleInput: PolicyRuleInput;
}

/**
 * 
 */
export class PolicyApi extends runtime.BaseAPI {

    /**
     * Creates request options for acknowledgePolicyImpact without sending the request
     */
    async acknowledgePolicyImpactRequestOpts(requestParameters: AcknowledgePolicyImpactRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling acknowledgePolicyImpact().'
            );
        }

        if (requestParameters['impactId'] == null) {
            throw new runtime.RequiredError(
                'impactId',
                'Required parameter "impactId" was null or undefined when calling acknowledgePolicyImpact().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}/impacts/{impact_id}/acknowledge`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));
        urlPath = urlPath.replace('{impact_id}', encodeURIComponent(String(requestParameters['impactId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * ``open`` -> ``acknowledged``.  404 if the impact doesn\'t exist (or belongs to another org/policy); 409 if it isn\'t ``open``. **Both** ``policy_id`` and ``impact_id`` are typed as UUIDs, so a malformed id in the URL 422s here rather than reaching the query and raising an unhandled ``asyncpg.DataError`` (=> 500) against a UUID column. This is deliberately stricter than ``admin_v2``\'s plain-``str`` path-param convention.
     * Acknowledge Policy Impact
     */
    async acknowledgePolicyImpactRaw(requestParameters: AcknowledgePolicyImpactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyImpactSummary>> {
        const requestOptions = await this.acknowledgePolicyImpactRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyImpactSummaryFromJSON(jsonValue));
    }

    /**
     * ``open`` -> ``acknowledged``.  404 if the impact doesn\'t exist (or belongs to another org/policy); 409 if it isn\'t ``open``. **Both** ``policy_id`` and ``impact_id`` are typed as UUIDs, so a malformed id in the URL 422s here rather than reaching the query and raising an unhandled ``asyncpg.DataError`` (=> 500) against a UUID column. This is deliberately stricter than ``admin_v2``\'s plain-``str`` path-param convention.
     * Acknowledge Policy Impact
     */
    async acknowledgePolicyImpact(requestParameters: AcknowledgePolicyImpactRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyImpactSummary> {
        const response = await this.acknowledgePolicyImpactRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for clonePolicy without sending the request
     */
    async clonePolicyRequestOpts(requestParameters: ClonePolicyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling clonePolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}/clone`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Clone an existing policy, creating a new policy with the same configuration but with a modified name (appending \" Copy\") and DRAFT status.
     * Clone Policy
     */
    async clonePolicyRaw(requestParameters: ClonePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyDetailResponse>> {
        const requestOptions = await this.clonePolicyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyDetailResponseFromJSON(jsonValue));
    }

    /**
     * Clone an existing policy, creating a new policy with the same configuration but with a modified name (appending \" Copy\") and DRAFT status.
     * Clone Policy
     */
    async clonePolicy(requestParameters: ClonePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyDetailResponse> {
        const response = await this.clonePolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for comparePolicyRevisions without sending the request
     */
    async comparePolicyRevisionsRequestOpts(requestParameters: ComparePolicyRevisionsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling comparePolicyRevisions().'
            );
        }

        if (requestParameters['from'] == null) {
            throw new runtime.RequiredError(
                'from',
                'Required parameter "from" was null or undefined when calling comparePolicyRevisions().'
            );
        }

        if (requestParameters['to'] == null) {
            throw new runtime.RequiredError(
                'to',
                'Required parameter "to" was null or undefined when calling comparePolicyRevisions().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}/revisions/compare`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Compare two revisions of a policy.  Returns the before/after state, a diff of changes, change categories, and the union of resolved_refs from both endpoint revisions. The before/after are always normalized so before is the older revision.
     * Compare Policy Revisions
     */
    async comparePolicyRevisionsRaw(requestParameters: ComparePolicyRevisionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyRevisionCompareResponse>> {
        const requestOptions = await this.comparePolicyRevisionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyRevisionCompareResponseFromJSON(jsonValue));
    }

    /**
     * Compare two revisions of a policy.  Returns the before/after state, a diff of changes, change categories, and the union of resolved_refs from both endpoint revisions. The before/after are always normalized so before is the older revision.
     * Compare Policy Revisions
     */
    async comparePolicyRevisions(requestParameters: ComparePolicyRevisionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyRevisionCompareResponse> {
        const response = await this.comparePolicyRevisionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for countPoliciesByAgent without sending the request
     */
    async countPoliciesByAgentRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/counts-by-agent`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return the number of policies associated with each agent.  Unscoped policies (applying to all agents) are counted separately.
     * Get Policy Counts By Agent
     */
    async countPoliciesByAgentRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AgentPolicyCounts>> {
        const requestOptions = await this.countPoliciesByAgentRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AgentPolicyCountsFromJSON(jsonValue));
    }

    /**
     * Return the number of policies associated with each agent.  Unscoped policies (applying to all agents) are counted separately.
     * Get Policy Counts By Agent
     */
    async countPoliciesByAgent(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AgentPolicyCounts> {
        const response = await this.countPoliciesByAgentRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createPolicy without sending the request
     */
    async createPolicyRequestOpts(requestParameters: CreatePolicyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createPolicy'] == null) {
            throw new runtime.RequiredError(
                'createPolicy',
                'Required parameter "createPolicy" was null or undefined when calling createPolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreatePolicyToJSON(requestParameters['createPolicy']),
        };
    }

    /**
     * Create a policy governing exactly one MCP server.  A rule grants its `actions` to its `roles_groups`. Access is denied by default, so a tool no rule grants is already blocked — `authorized: false` is only for carving an exception out of a broader allow.  Write ONE rule per `roles_groups` set — all of a principal\'s actions on one rule, with `:approve` carrying allow-vs-confirm. Example, reads free and writes confirmed:      {\"name\": \"Support tooling\", \"mcp_server_id\": \"3f7c…\", \"rules\": [       {\"name\": \"Support issue access\", \"authorized\": true, \"active\": true,        \"actions\": [\"tools/call:list_issues\", \"tools/call:create_issue:approve\"],        \"roles_groups\": [\"group:Support\"]}]}  One policy per (server, agent scope): a second ACTIVE policy over the same `mcp_server_id` and `application_ids` is refused with 409 naming the holder. Add rules to the existing policy instead, or scope the new one to specific `application_ids`. `validate_policy` reports this before you write.
     * Create Policy
     */
    async createPolicyRaw(requestParameters: CreatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyDetailResponse>> {
        const requestOptions = await this.createPolicyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyDetailResponseFromJSON(jsonValue));
    }

    /**
     * Create a policy governing exactly one MCP server.  A rule grants its `actions` to its `roles_groups`. Access is denied by default, so a tool no rule grants is already blocked — `authorized: false` is only for carving an exception out of a broader allow.  Write ONE rule per `roles_groups` set — all of a principal\'s actions on one rule, with `:approve` carrying allow-vs-confirm. Example, reads free and writes confirmed:      {\"name\": \"Support tooling\", \"mcp_server_id\": \"3f7c…\", \"rules\": [       {\"name\": \"Support issue access\", \"authorized\": true, \"active\": true,        \"actions\": [\"tools/call:list_issues\", \"tools/call:create_issue:approve\"],        \"roles_groups\": [\"group:Support\"]}]}  One policy per (server, agent scope): a second ACTIVE policy over the same `mcp_server_id` and `application_ids` is refused with 409 naming the holder. Add rules to the existing policy instead, or scope the new one to specific `application_ids`. `validate_policy` reports this before you write.
     * Create Policy
     */
    async createPolicy(requestParameters: CreatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyDetailResponse> {
        const response = await this.createPolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getPolicy without sending the request
     */
    async getPolicyRequestOpts(requestParameters: GetPolicyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling getPolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Get Policy
     */
    async getPolicyRaw(requestParameters: GetPolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyDetailResponse>> {
        const requestOptions = await this.getPolicyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyDetailResponseFromJSON(jsonValue));
    }

    /**
     * Get Policy
     */
    async getPolicy(requestParameters: GetPolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyDetailResponse> {
        const response = await this.getPolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getPolicyImpactsSummary without sending the request
     */
    async getPolicyImpactsSummaryRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policy-impacts/summary`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Badge counts: ``{policy_id: {breaking, review, info}}``, archived excluded.
     * Get Policy Impacts Summary
     */
    async getPolicyImpactsSummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: PolicyImpactSeverityCounts; }>> {
        const requestOptions = await this.getPolicyImpactsSummaryRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => runtime.mapValues(jsonValue, PolicyImpactSeverityCountsFromJSON));
    }

    /**
     * Badge counts: ``{policy_id: {breaking, review, info}}``, archived excluded.
     * Get Policy Impacts Summary
     */
    async getPolicyImpactsSummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: PolicyImpactSeverityCounts; }> {
        const response = await this.getPolicyImpactsSummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getPolicyImpactsSummaryByKind without sending the request
     */
    async getPolicyImpactsSummaryByKindRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policy-impacts/summary/by-kind`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Rollup line counts: ``{policy_id: {change_kind: live_count}}``, archived excluded.  Sibling of ``/policy-impacts/summary`` grouped by ``change_kind`` instead of severity — kept as a separate route rather than a widened ``/summary`` response because ``PolicyImpactSeverityCounts`` uses ``extra=\"allow\"`` to surface unrecognised severities, and a structural ``kinds`` key there would be indistinguishable from one. ``change_kind`` is tool-router\'s open vocabulary, so kinds are not zero-filled: only kinds with live rows appear.
     * Get Policy Impacts Summary By Kind
     */
    async getPolicyImpactsSummaryByKindRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: { [key: string]: number; }; }>> {
        const requestOptions = await this.getPolicyImpactsSummaryByKindRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Rollup line counts: ``{policy_id: {change_kind: live_count}}``, archived excluded.  Sibling of ``/policy-impacts/summary`` grouped by ``change_kind`` instead of severity — kept as a separate route rather than a widened ``/summary`` response because ``PolicyImpactSeverityCounts`` uses ``extra=\"allow\"`` to surface unrecognised severities, and a structural ``kinds`` key there would be indistinguishable from one. ``change_kind`` is tool-router\'s open vocabulary, so kinds are not zero-filled: only kinds with live rows appear.
     * Get Policy Impacts Summary By Kind
     */
    async getPolicyImpactsSummaryByKind(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: { [key: string]: number; }; }> {
        const response = await this.getPolicyImpactsSummaryByKindRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getPolicyRevision without sending the request
     */
    async getPolicyRevisionRequestOpts(requestParameters: GetPolicyRevisionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling getPolicyRevision().'
            );
        }

        if (requestParameters['revisionId'] == null) {
            throw new runtime.RequiredError(
                'revisionId',
                'Required parameter "revisionId" was null or undefined when calling getPolicyRevision().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}/revisions/{revision_id}`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));
        urlPath = urlPath.replace('{revision_id}', encodeURIComponent(String(requestParameters['revisionId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return a single policy revision by ID, scoped to the given policy.
     * Get Policy Revision
     */
    async getPolicyRevisionRaw(requestParameters: GetPolicyRevisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyRevisionDetailResponse>> {
        const requestOptions = await this.getPolicyRevisionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyRevisionDetailResponseFromJSON(jsonValue));
    }

    /**
     * Return a single policy revision by ID, scoped to the given policy.
     * Get Policy Revision
     */
    async getPolicyRevision(requestParameters: GetPolicyRevisionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyRevisionDetailResponse> {
        const response = await this.getPolicyRevisionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getPolicySummary without sending the request
     */
    async getPolicySummaryRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/summary`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return summary counts of policies by status.
     * Get Policies Summary
     */
    async getPolicySummaryRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicySummaryResponse>> {
        const requestOptions = await this.getPolicySummaryRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicySummaryResponseFromJSON(jsonValue));
    }

    /**
     * Return summary counts of policies by status.
     * Get Policies Summary
     */
    async getPolicySummary(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicySummaryResponse> {
        const response = await this.getPolicySummaryRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listAllPolicyImpacts without sending the request
     */
    async listAllPolicyImpactsRequestOpts(requestParameters: ListAllPolicyImpactsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['severity'] != null) {
            queryParameters['severity'] = requestParameters['severity'];
        }

        if (requestParameters['policyId'] != null) {
            queryParameters['policy_id'] = requestParameters['policyId'];
        }

        if (requestParameters['mcpServerId'] != null) {
            queryParameters['mcp_server_id'] = requestParameters['mcpServerId'];
        }

        if (requestParameters['snapshotId'] != null) {
            queryParameters['snapshot_id'] = requestParameters['snapshotId'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policy-impacts`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Org-wide impact list.  Serves the ACC org view and the server/snapshot revisions page (hence the ``mcp_server_id`` / ``snapshot_id`` filters on what is otherwise an org-wide list). Archived policies are excluded by :class:`PolicyImpactService`\'s join on ``policy.status``.
     * List Policy Impacts
     */
    async listAllPolicyImpactsRaw(requestParameters: ListAllPolicyImpactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PaginationResponsePolicyImpactSummary>> {
        const requestOptions = await this.listAllPolicyImpactsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PaginationResponsePolicyImpactSummaryFromJSON(jsonValue));
    }

    /**
     * Org-wide impact list.  Serves the ACC org view and the server/snapshot revisions page (hence the ``mcp_server_id`` / ``snapshot_id`` filters on what is otherwise an org-wide list). Archived policies are excluded by :class:`PolicyImpactService`\'s join on ``policy.status``.
     * List Policy Impacts
     */
    async listAllPolicyImpacts(requestParameters: ListAllPolicyImpactsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PaginationResponsePolicyImpactSummary> {
        const response = await this.listAllPolicyImpactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listPolicies without sending the request
     */
    async listPoliciesRequestOpts(requestParameters: ListPoliciesRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['mcpServerId'] != null) {
            queryParameters['mcp_server_id'] = requestParameters['mcpServerId'];
        }

        if (requestParameters['agentId'] != null) {
            queryParameters['agent_id'] = requestParameters['agentId'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sort_by'] = requestParameters['sortBy'];
        }

        if (requestParameters['sortOrder'] != null) {
            queryParameters['sort_order'] = requestParameters['sortOrder'];
        }

        if (requestParameters['hasImpacts'] != null) {
            queryParameters['has_impacts'] = requestParameters['hasImpacts'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List Policies
     */
    async listPoliciesRaw(requestParameters: ListPoliciesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PaginationResponsePolicySummary>> {
        const requestOptions = await this.listPoliciesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PaginationResponsePolicySummaryFromJSON(jsonValue));
    }

    /**
     * List Policies
     */
    async listPolicies(requestParameters: ListPoliciesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PaginationResponsePolicySummary> {
        const response = await this.listPoliciesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listPoliciesAffectedBy without sending the request
     */
    async listPoliciesAffectedByRequestOpts(requestParameters: ListPoliciesAffectedByRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['agentId'] != null) {
            queryParameters['agent_id'] = requestParameters['agentId'];
        }

        if (requestParameters['mcpServerId'] != null) {
            queryParameters['mcp_server_id'] = requestParameters['mcpServerId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/affected-by`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Preview policies that would be archived or revised by a cascade.  Backs the AI agent / MCP server delete-confirmation modal so the user sees what\'s about to change before they confirm (BCP-1686 spec § \"Policy-service endpoint\"). Exactly one of ``agent_id`` / ``mcp_server_id`` must be supplied.
     * Get Affected Policies
     */
    async listPoliciesAffectedByRaw(requestParameters: ListPoliciesAffectedByRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AffectedPoliciesResponse>> {
        const requestOptions = await this.listPoliciesAffectedByRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AffectedPoliciesResponseFromJSON(jsonValue));
    }

    /**
     * Preview policies that would be archived or revised by a cascade.  Backs the AI agent / MCP server delete-confirmation modal so the user sees what\'s about to change before they confirm (BCP-1686 spec § \"Policy-service endpoint\"). Exactly one of ``agent_id`` / ``mcp_server_id`` must be supplied.
     * Get Affected Policies
     */
    async listPoliciesAffectedBy(requestParameters: ListPoliciesAffectedByRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AffectedPoliciesResponse> {
        const response = await this.listPoliciesAffectedByRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listPolicyFilterOptions without sending the request
     */
    async listPolicyFilterOptionsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/filter-definitions`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Return filter categories for the policies list UI.  Static filter options for status, plus dynamic filters for MCP servers and agents based on the organization\'s registry.
     * Get Filter Definitions
     */
    async listPolicyFilterOptionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<FilterCategory>>> {
        const requestOptions = await this.listPolicyFilterOptionsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(FilterCategoryFromJSON));
    }

    /**
     * Return filter categories for the policies list UI.  Static filter options for status, plus dynamic filters for MCP servers and agents based on the organization\'s registry.
     * Get Filter Definitions
     */
    async listPolicyFilterOptions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<FilterCategory>> {
        const response = await this.listPolicyFilterOptionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listPolicyImpacts without sending the request
     */
    async listPolicyImpactsRequestOpts(requestParameters: ListPolicyImpactsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling listPolicyImpacts().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}/impacts`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Every impact on one policy, newest first — the policy detail view\'s list.  Unlike the org-wide list, resolved rows are NOT excluded by default: this is the history view, with resolved rows collapsed client-side. Archived policies are still excluded (:class:`PolicyImpactService`\'s ``_base_query`` backs both this and the org-wide list identically) — an archived policy\'s detail page must not surface flags the org view is hiding.  **Capped, and the cap is not an error.** At most :data:`~policy_service.services.policy_impact.PER_POLICY_LIST_CAP` rows are returned, newest first. Resolved impacts are kept indefinitely as an audit trail and each carries a non-trivial ``detail`` blob, so an uncapped response would grow without limit. Truncation is therefore possible and is NOT signalled — this is a detail view where the newest rows are what matter. For full history, page the org-wide ``GET /policy-impacts?policy_id=`` instead.
     * List Impacts For Policy
     */
    async listPolicyImpactsRaw(requestParameters: ListPolicyImpactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<PolicyImpactSummary>>> {
        const requestOptions = await this.listPolicyImpactsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(PolicyImpactSummaryFromJSON));
    }

    /**
     * Every impact on one policy, newest first — the policy detail view\'s list.  Unlike the org-wide list, resolved rows are NOT excluded by default: this is the history view, with resolved rows collapsed client-side. Archived policies are still excluded (:class:`PolicyImpactService`\'s ``_base_query`` backs both this and the org-wide list identically) — an archived policy\'s detail page must not surface flags the org view is hiding.  **Capped, and the cap is not an error.** At most :data:`~policy_service.services.policy_impact.PER_POLICY_LIST_CAP` rows are returned, newest first. Resolved impacts are kept indefinitely as an audit trail and each carries a non-trivial ``detail`` blob, so an uncapped response would grow without limit. Truncation is therefore possible and is NOT signalled — this is a detail view where the newest rows are what matter. For full history, page the org-wide ``GET /policy-impacts?policy_id=`` instead.
     * List Impacts For Policy
     */
    async listPolicyImpacts(requestParameters: ListPolicyImpactsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<PolicyImpactSummary>> {
        const response = await this.listPolicyImpactsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listPolicyRevisions without sending the request
     */
    async listPolicyRevisionsRequestOpts(requestParameters: ListPolicyRevisionsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling listPolicyRevisions().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['category'] != null) {
            queryParameters['category'] = requestParameters['category'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}/revisions`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * List all revisions for a given policy with pagination.
     * List Policy Revisions
     */
    async listPolicyRevisionsRaw(requestParameters: ListPolicyRevisionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PaginationResponsePolicyRevisionSummary>> {
        const requestOptions = await this.listPolicyRevisionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PaginationResponsePolicyRevisionSummaryFromJSON(jsonValue));
    }

    /**
     * List all revisions for a given policy with pagination.
     * List Policy Revisions
     */
    async listPolicyRevisions(requestParameters: ListPolicyRevisionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PaginationResponsePolicyRevisionSummary> {
        const response = await this.listPolicyRevisionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updatePolicy without sending the request
     */
    async updatePolicyRequestOpts(requestParameters: UpdatePolicyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyId'] == null) {
            throw new runtime.RequiredError(
                'policyId',
                'Required parameter "policyId" was null or undefined when calling updatePolicy().'
            );
        }

        if (requestParameters['updatePolicy'] == null) {
            throw new runtime.RequiredError(
                'updatePolicy',
                'Required parameter "updatePolicy" was null or undefined when calling updatePolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/{policy_id}`;
        urlPath = urlPath.replace('{policy_id}', encodeURIComponent(String(requestParameters['policyId'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: UpdatePolicyToJSON(requestParameters['updatePolicy']),
        };
    }

    /**
     * Update a policy. Sending `rules` REPLACES the entire rule list.  There is no \"add a rule\" operation. To add one, read the policy first and send every existing rule back alongside the new one — sending only the new rule deletes the others, and the call still returns 200 with a policy that looks perfectly healthy. Omit `rules` entirely to change only metadata.  Verify afterwards rather than assuming: `GET /v2/policies/{policy_id}/revisions` lists what each change actually did, and `compare_policy_revisions` diffs two of them. A revision summarising removals you did not intend is how you catch a partial send — and it is also how you recover, since the diff records what was taken out.  Other fields are a partial update: omitted ones are left alone. `mcp_server_id` is immutable — rules reference server-specific tool actions, so retargeting is a new policy, not an edit.
     * Update Policy
     */
    async updatePolicyRaw(requestParameters: UpdatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PolicyDetailResponse>> {
        const requestOptions = await this.updatePolicyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PolicyDetailResponseFromJSON(jsonValue));
    }

    /**
     * Update a policy. Sending `rules` REPLACES the entire rule list.  There is no \"add a rule\" operation. To add one, read the policy first and send every existing rule back alongside the new one — sending only the new rule deletes the others, and the call still returns 200 with a policy that looks perfectly healthy. Omit `rules` entirely to change only metadata.  Verify afterwards rather than assuming: `GET /v2/policies/{policy_id}/revisions` lists what each change actually did, and `compare_policy_revisions` diffs two of them. A revision summarising removals you did not intend is how you catch a partial send — and it is also how you recover, since the diff records what was taken out.  Other fields are a partial update: omitted ones are left alone. `mcp_server_id` is immutable — rules reference server-specific tool actions, so retargeting is a new policy, not an edit.
     * Update Policy
     */
    async updatePolicy(requestParameters: UpdatePolicyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PolicyDetailResponse> {
        const response = await this.updatePolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for validatePolicy without sending the request
     */
    async validatePolicyRequestOpts(requestParameters: ValidatePolicyOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['validatePolicyRequest'] == null) {
            throw new runtime.RequiredError(
                'validatePolicyRequest',
                'Required parameter "validatePolicyRequest" was null or undefined when calling validatePolicy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/validate`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ValidatePolicyRequestToJSON(requestParameters['validatePolicyRequest']),
        };
    }

    /**
     * Validate a policy before creation or update. Checks for duplicate names and overlapping MCP server/agent combinations. When exclude_policy_id is provided (edit mode), that policy is excluded from validation.
     * Validate Policy
     */
    async validatePolicyRaw(requestParameters: ValidatePolicyOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ValidatePolicyResponse>> {
        const requestOptions = await this.validatePolicyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ValidatePolicyResponseFromJSON(jsonValue));
    }

    /**
     * Validate a policy before creation or update. Checks for duplicate names and overlapping MCP server/agent combinations. When exclude_policy_id is provided (edit mode), that policy is excluded from validation.
     * Validate Policy
     */
    async validatePolicy(requestParameters: ValidatePolicyOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ValidatePolicyResponse> {
        const response = await this.validatePolicyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for validatePolicyRule without sending the request
     */
    async validatePolicyRuleRequestOpts(requestParameters: ValidatePolicyRuleRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['policyRuleInput'] == null) {
            throw new runtime.RequiredError(
                'policyRuleInput',
                'Required parameter "policyRuleInput" was null or undefined when calling validatePolicyRule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/policy/v2/policies/rule/validate`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PolicyRuleInputToJSON(requestParameters['policyRuleInput']),
        };
    }

    /**
     * Validate a single policy rule for correctness.  Validates CEL expression syntax, undefined variables, action format, and role/group format. Returns 204 No Content if valid, or 422 with Pydantic validation errors if invalid.
     * Validate Rule
     */
    async validatePolicyRuleRaw(requestParameters: ValidatePolicyRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.validatePolicyRuleRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Validate a single policy rule for correctness.  Validates CEL expression syntax, undefined variables, action format, and role/group format. Returns 204 No Content if valid, or 422 with Pydantic validation errors if invalid.
     * Validate Rule
     */
    async validatePolicyRule(requestParameters: ValidatePolicyRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.validatePolicyRuleRaw(requestParameters, initOverrides);
    }

}

/**
 * @export
 */
export const ListPoliciesSortByEnum = {
    Name: 'name',
    McpServerName: 'mcp_server_name',
    Status: 'status',
    UpdatedAt: 'updated_at',
} as const;
export type ListPoliciesSortByEnum = typeof ListPoliciesSortByEnum[keyof typeof ListPoliciesSortByEnum];
/**
 * @export
 */
export const ListPoliciesSortOrderEnum = {
    Asc: 'asc',
    Desc: 'desc',
} as const;
export type ListPoliciesSortOrderEnum = typeof ListPoliciesSortOrderEnum[keyof typeof ListPoliciesSortOrderEnum];
