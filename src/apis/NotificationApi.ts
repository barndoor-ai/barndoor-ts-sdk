/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ChannelListResponse,
    ChannelListResponseFromJSON,
    ChannelListResponseToJSON,
} from '../models/ChannelListResponse.js';
import {
    type ChannelOptionsResponse,
    ChannelOptionsResponseFromJSON,
    ChannelOptionsResponseToJSON,
} from '../models/ChannelOptionsResponse.js';
import {
    type ChannelResponse,
    ChannelResponseFromJSON,
    ChannelResponseToJSON,
} from '../models/ChannelResponse.js';
import {
    type ChannelTestResponse,
    ChannelTestResponseFromJSON,
    ChannelTestResponseToJSON,
} from '../models/ChannelTestResponse.js';
import {
    type ChannelUpsertRequest,
    ChannelUpsertRequestFromJSON,
    ChannelUpsertRequestToJSON,
} from '../models/ChannelUpsertRequest.js';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError.js';
import {
    type WebhookSecretResponse,
    WebhookSecretResponseFromJSON,
    WebhookSecretResponseToJSON,
} from '../models/WebhookSecretResponse.js';

export interface DeleteAlertChannelRequest {
    /**
     * 
     */
    channelId: string;
}

export interface RegenerateAlertChannelSecretRequest {
    /**
     * 
     */
    channelId: string;
}

export interface TestAlertChannelRequest {
    /**
     * 
     */
    channelId: string;
}

export interface UpsertAlertChannelRequest {
    /**
     * 
     */
    channelUpsertRequest: ChannelUpsertRequest;
}

/**
 * 
 */
export class NotificationApi extends runtime.BaseAPI {

    /**
     * Creates request options for deleteAlertChannel without sending the request
     */
    async deleteAlertChannelRequestOpts(requestParameters: DeleteAlertChannelRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['channelId'] == null) {
            throw new runtime.RequiredError(
                'channelId',
                'Required parameter "channelId" was null or undefined when calling deleteAlertChannel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/notification/public/v1/channels/{channel_id}`;
        urlPath = urlPath.replace('{channel_id}', encodeURIComponent(String(requestParameters['channelId'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Deletes one channel; its subscriptions cascade and any stored secret is removed. Irreversible — to stop delivery reversibly, upsert the channel with `enabled: false` instead.
     * Delete a channel
     */
    async deleteAlertChannelRaw(requestParameters: DeleteAlertChannelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteAlertChannelRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes one channel; its subscriptions cascade and any stored secret is removed. Irreversible — to stop delivery reversibly, upsert the channel with `enabled: false` instead.
     * Delete a channel
     */
    async deleteAlertChannel(requestParameters: DeleteAlertChannelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteAlertChannelRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getMyAlertChannel without sending the request
     */
    async getMyAlertChannelRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/notification/public/v1/channels/user`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the authenticated caller\'s own `in_app` and `user_email` channels. Always self-scoped — there is no way to read another user\'s personal channels, and no user parameter to pass.
     * List the caller\'s personal channels
     */
    async getMyAlertChannelRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChannelListResponse>> {
        const requestOptions = await this.getMyAlertChannelRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChannelListResponseFromJSON(jsonValue));
    }

    /**
     * Returns the authenticated caller\'s own `in_app` and `user_email` channels. Always self-scoped — there is no way to read another user\'s personal channels, and no user parameter to pass.
     * List the caller\'s personal channels
     */
    async getMyAlertChannel(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChannelListResponse> {
        const response = await this.getMyAlertChannelRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listAlertChannelOptions without sending the request
     */
    async listAlertChannelOptionsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/notification/public/v1/channels/options`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns every alert type this organization may subscribe a channel to, plus the category and severity label vocabularies those types reference.  Read this before building a subscription set. The alert-type enum grows over time and is gated per organization, so this endpoint — not a hardcoded list — is the authoritative answer to what `subscriptions[].alert_type` accepts. Subscribing to a type absent here is accepted but never delivers.
     * List the subscribable alert vocabulary
     */
    async listAlertChannelOptionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChannelOptionsResponse>> {
        const requestOptions = await this.listAlertChannelOptionsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChannelOptionsResponseFromJSON(jsonValue));
    }

    /**
     * Returns every alert type this organization may subscribe a channel to, plus the category and severity label vocabularies those types reference.  Read this before building a subscription set. The alert-type enum grows over time and is gated per organization, so this endpoint — not a hardcoded list — is the authoritative answer to what `subscriptions[].alert_type` accepts. Subscribing to a type absent here is accepted but never delivers.
     * List the subscribable alert vocabulary
     */
    async listAlertChannelOptions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChannelOptionsResponse> {
        const response = await this.listAlertChannelOptionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listAlertChannels without sending the request
     */
    async listAlertChannelsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/notification/public/v1/channels`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the organization\'s org-wide channels — `email`, `webhook`, `slack`, `teams` — with their current subscription sets. Personal channels are not included here; read those from `/channels/user`. Unpaginated. Secrets are never returned: a webhook\'s signing secret and a Teams workflow URL surface only as the `has_signing_secret` / `has_workflow_url` flags.
     * List the organization\'s shared channels
     */
    async listAlertChannelsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChannelListResponse>> {
        const requestOptions = await this.listAlertChannelsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChannelListResponseFromJSON(jsonValue));
    }

    /**
     * Returns the organization\'s org-wide channels — `email`, `webhook`, `slack`, `teams` — with their current subscription sets. Personal channels are not included here; read those from `/channels/user`. Unpaginated. Secrets are never returned: a webhook\'s signing secret and a Teams workflow URL surface only as the `has_signing_secret` / `has_workflow_url` flags.
     * List the organization\'s shared channels
     */
    async listAlertChannels(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChannelListResponse> {
        const response = await this.listAlertChannelsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for regenerateAlertChannelSecret without sending the request
     */
    async regenerateAlertChannelSecretRequestOpts(requestParameters: RegenerateAlertChannelSecretRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['channelId'] == null) {
            throw new runtime.RequiredError(
                'channelId',
                'Required parameter "channelId" was null or undefined when calling regenerateAlertChannelSecret().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/notification/public/v1/channels/{channel_id}/regenerate-secret`;
        urlPath = urlPath.replace('{channel_id}', encodeURIComponent(String(requestParameters['channelId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Generates a new Standard Webhooks signing secret for a `webhook` channel and returns it once, in this response. The previous secret stops verifying immediately, so deploy the new one to your receiver before the next alert fires. Use this if the secret from channel creation was lost or needs rotating; there is no endpoint that reads the current secret.
     * Rotate a webhook channel\'s signing secret
     */
    async regenerateAlertChannelSecretRaw(requestParameters: RegenerateAlertChannelSecretRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WebhookSecretResponse>> {
        const requestOptions = await this.regenerateAlertChannelSecretRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WebhookSecretResponseFromJSON(jsonValue));
    }

    /**
     * Generates a new Standard Webhooks signing secret for a `webhook` channel and returns it once, in this response. The previous secret stops verifying immediately, so deploy the new one to your receiver before the next alert fires. Use this if the secret from channel creation was lost or needs rotating; there is no endpoint that reads the current secret.
     * Rotate a webhook channel\'s signing secret
     */
    async regenerateAlertChannelSecret(requestParameters: RegenerateAlertChannelSecretRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WebhookSecretResponse> {
        const response = await this.regenerateAlertChannelSecretRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for testAlertChannel without sending the request
     */
    async testAlertChannelRequestOpts(requestParameters: TestAlertChannelRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['channelId'] == null) {
            throw new runtime.RequiredError(
                'channelId',
                'Required parameter "channelId" was null or undefined when calling testAlertChannel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/notification/public/v1/channels/{channel_id}/test`;
        urlPath = urlPath.replace('{channel_id}', encodeURIComponent(String(requestParameters['channelId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Sends a stock connectivity-test message through the channel\'s real transport, so you can verify a webhook receiver, email address, Slack channel, or Teams workflow actually works. It is not an alert: nothing is persisted, no other channel is notified, and it does not depend on the channel\'s subscriptions.  A transport failure is reported as `200` with `ok: false` and an `error` string — the request succeeded, the delivery did not.
     * Send a test message through a channel
     */
    async testAlertChannelRaw(requestParameters: TestAlertChannelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChannelTestResponse>> {
        const requestOptions = await this.testAlertChannelRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChannelTestResponseFromJSON(jsonValue));
    }

    /**
     * Sends a stock connectivity-test message through the channel\'s real transport, so you can verify a webhook receiver, email address, Slack channel, or Teams workflow actually works. It is not an alert: nothing is persisted, no other channel is notified, and it does not depend on the channel\'s subscriptions.  A transport failure is reported as `200` with `ok: false` and an `error` string — the request succeeded, the delivery did not.
     * Send a test message through a channel
     */
    async testAlertChannel(requestParameters: TestAlertChannelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChannelTestResponse> {
        const response = await this.testAlertChannelRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for upsertAlertChannel without sending the request
     */
    async upsertAlertChannelRequestOpts(requestParameters: UpsertAlertChannelRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['channelUpsertRequest'] == null) {
            throw new runtime.RequiredError(
                'channelUpsertRequest',
                'Required parameter "channelUpsertRequest" was null or undefined when calling upsertAlertChannel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            const oauthToken = await this.configuration.accessToken("OAuth2", ["openid", "profile", "email"]);
            if (oauthToken) {
                headerParameters["Authorization"] = oauthToken.startsWith("Bearer ") ? oauthToken : `Bearer ${oauthToken}`;
            }
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKey", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/notification/public/v1/channels`;

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ChannelUpsertRequestToJSON(requestParameters['channelUpsertRequest']),
        };
    }

    /**
     * Upserts one channel and **replaces** its subscription set.  Without `id`, the channel is keyed on its type\'s natural identity, so an identical call is idempotent rather than duplicating: `email` by address, `webhook` by URL, `slack` by channel id, personal types by (organization, type, caller). With `id` it is an authoritative edit of that row — the only way to update a `teams` channel, whose natural identity is a secret URL.  `subscriptions` is replaced wholesale, not merged: send the complete desired set, and expect an omitted list to unsubscribe the channel from everything.  Creating a `webhook` channel reveals its signing secret in `signing_secret` ONCE; it is not readable afterwards. The secret is only generated when the row is new, so a retried create that lands after the first succeeded returns `signing_secret: null`. If you need a secret and got `null`, call `POST /channels/{channel_id}/regenerate-secret`.
     * Create or update a notification channel
     */
    async upsertAlertChannelRaw(requestParameters: UpsertAlertChannelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ChannelResponse>> {
        const requestOptions = await this.upsertAlertChannelRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ChannelResponseFromJSON(jsonValue));
    }

    /**
     * Upserts one channel and **replaces** its subscription set.  Without `id`, the channel is keyed on its type\'s natural identity, so an identical call is idempotent rather than duplicating: `email` by address, `webhook` by URL, `slack` by channel id, personal types by (organization, type, caller). With `id` it is an authoritative edit of that row — the only way to update a `teams` channel, whose natural identity is a secret URL.  `subscriptions` is replaced wholesale, not merged: send the complete desired set, and expect an omitted list to unsubscribe the channel from everything.  Creating a `webhook` channel reveals its signing secret in `signing_secret` ONCE; it is not readable afterwards. The secret is only generated when the row is new, so a retried create that lands after the first succeeded returns `signing_secret: null`. If you need a secret and got `null`, call `POST /channels/{channel_id}/regenerate-secret`.
     * Create or update a notification channel
     */
    async upsertAlertChannel(requestParameters: UpsertAlertChannelRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ChannelResponse> {
        const response = await this.upsertAlertChannelRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
