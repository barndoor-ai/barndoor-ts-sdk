/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues, parseDate, parseDateTime, serializeDate, serializeDateTime } from '../runtime.js';
import type { BillingReason } from './BillingReason.js';
import {
    BillingReasonFromJSON,
    BillingReasonFromJSONTyped,
    BillingReasonToJSON,
    BillingReasonToJSONTyped,
} from './BillingReason.js';
import type { BillingMode } from './BillingMode.js';
import {
    BillingModeFromJSON,
    BillingModeFromJSONTyped,
    BillingModeToJSON,
    BillingModeToJSONTyped,
} from './BillingMode.js';
import type { ModelSyncMode } from './ModelSyncMode.js';
import {
    ModelSyncModeFromJSON,
    ModelSyncModeFromJSONTyped,
    ModelSyncModeToJSON,
    ModelSyncModeToJSONTyped,
} from './ModelSyncMode.js';

/**
 * 
 * @export
 * @interface Provider
 */
export interface Provider {
    /**
     * 
     */
    createdByEmail?: string | null;
    /**
     * 
     */
    createdByName?: string | null;
    /**
     * 
     */
    createdByUserId?: string | null;
    /**
     * Optional free text explaining *why* the last change was made
     * ("Raised for the Q3 batch job", "Vendor deprecated the old endpoint").
     * 
     * Admin-authored and rendered in the portal, so it is untrusted input:
     * length-capped here, escaped on render.
     */
    lastChangeNote?: string | null;
    /**
     * When the change named by `last_modified_by_*` happened.
     * 
     * Its own column rather than the row's `updated_at`, because `updated_at`
     * is bumped by writes nobody is attributed for -- pairing the two would
     * date a named admin's edit to a background job's clock. See the V75
     * migration for the full reasoning.
     */
    lastModifiedAt?: Date | null;
    /**
     * 
     */
    lastModifiedByEmail?: string | null;
    /**
     * 
     */
    lastModifiedByName?: string | null;
    /**
     * 
     */
    lastModifiedByUserId?: string | null;
    /**
     * 
     */
    authType: string;
    /**
     * 
     */
    baseUrl: string;
    /**
     * Whether the gateway records a per-token cost for this provider's
     * traffic (V69 / BCP-3876). `PerToken` for every provider that has not
     * opted in, which is the default and the back-compat value.
     * 
     * The request path does **not** read this field: billing consults
     * [`ai_governance::CostCalculator`]'s own cached not-metered set, which
     * is refreshed from the same query as pricing. This exists for the admin
     * API surface and the audit trail.
     */
    billingMode: BillingMode;
    /**
     * Optional free-text context for the arrangement (max 200 chars).
     * Human-readable only — never parsed, never aggregated.
     */
    billingNote?: string | null;
    /**
     * How the vendor bills this provider. Required when `billing_mode` is
     * `NotMetered`, optional otherwise (V69 CHECK-enforced) — a subscription
     * billing overages is both per-token and a subscription. Purely
     * descriptive: no code branches on it, which is what lets a new reason be
     * added without touching the billing path.
     */
    billingReason?: BillingReason | null;
    /**
     * 
     */
    catalogId?: string | null;
    /**
     * 
     */
    catalogSlug?: string | null;
    /**
     * 
     */
    connectionId?: string | null;
    /**
     * 
     */
    createdAt: Date;
    /**
     * 
     */
    enabled: boolean;
    /**
     * Operator override for the health gate (V46). When `true` (default)
     * routing requires `health_status <> 'unhealthy'`; when `false` the gate
     * is bypassed and the provider serves regardless of probe results (we
     * still probe and surface health). Lets an admin force traffic through a
     * false-positive probe.
     */
    enforceHealthCheck: boolean;
    /**
     * When the last connectivity probe ran (NULL until the first probe).
     */
    healthCheckedAt?: Date | null;
    /**
     * Human-readable reason for the last `unhealthy` probe (NULL otherwise).
     */
    healthDetail?: string | null;
    /**
     * Observed upstream reachability, recorded separately from `enabled`
     * (operator intent) so the system never overwrites the toggle (V45).
     * One of `unverified` | `healthy` | `unhealthy`. `unverified` is the
     * back-compat default and also covers provider families we don't probe
     * (Bedrock/Vertex/Azure, claude_oauth, codex_oauth). A provider serves traffic only
     * when `enabled` AND `health_status <> 'unhealthy'`: a failed probe
     * auto-suspends it from routing (see `resolve_*`) without disabling it,
     * and it resumes once a probe records it healthy again.
     */
    healthStatus: string;
    /**
     * 
     */
    id: string;
    /**
     * 
     */
    modelProvider: string;
    /**
     * Whether this provider tracks Barndoor's model catalog, and how far
     * (V63 / BCP-3811). `Off` for every provider that has not opted in,
     * which is the default and the back-compat value.
     */
    modelSyncMode: ModelSyncMode;
    /**
     * 
     */
    name: string;
    /**
     * 
     */
    orgId: string;
    /**
     * Provider-tier total-request timeout override (non-streaming).
     * Operator-only and `None` by default: the effective value is written
     * at the route tier (`model_mappings`), so this only applies if a route
     * row somehow leaves it unset. Range
     * `TOTAL_REQUEST_TIMEOUT_MIN_SECS..=TOTAL_REQUEST_TIMEOUT_MAX_SECS` is
     * enforced by the CHECK constraint (see `proxy::timeouts`).
     */
    requestTimeoutSecs?: number | null;
    /**
     * 
     */
    secretPath: string;
    /**
     * 
     */
    settings: any | null;
    /**
     * Provider-tier stream-idle timeout override (streaming). Operator-only
     * and `None` by default; the effective value is written at the route
     * tier (`model_mappings`). Range
     * `STREAM_IDLE_TIMEOUT_MIN_SECS..=STREAM_IDLE_TIMEOUT_MAX_SECS` is
     * enforced by the CHECK constraint (see `proxy::timeouts`).
     */
    streamIdleTimeoutSecs?: number | null;
    /**
     * 
     */
    updatedAt: Date;
}



/**
 * Check if a given object implements the Provider interface.
 */
export function instanceOfProvider(value: object): value is Provider {
    if ((!('authType' in (value as Record<string, any>)) && !('auth_type' in (value as Record<string, any>))) || ((value as Record<string, any>)['authType'] === undefined && (value as Record<string, any>)['auth_type'] === undefined)) return false;
    if ((!('baseUrl' in (value as Record<string, any>)) && !('base_url' in (value as Record<string, any>))) || ((value as Record<string, any>)['baseUrl'] === undefined && (value as Record<string, any>)['base_url'] === undefined)) return false;
    if ((!('billingMode' in (value as Record<string, any>)) && !('billing_mode' in (value as Record<string, any>))) || ((value as Record<string, any>)['billingMode'] === undefined && (value as Record<string, any>)['billing_mode'] === undefined)) return false;
    if ((!('createdAt' in (value as Record<string, any>)) && !('created_at' in (value as Record<string, any>))) || ((value as Record<string, any>)['createdAt'] === undefined && (value as Record<string, any>)['created_at'] === undefined)) return false;
    if (!('enabled' in value) || value['enabled'] === undefined) return false;
    if ((!('enforceHealthCheck' in (value as Record<string, any>)) && !('enforce_health_check' in (value as Record<string, any>))) || ((value as Record<string, any>)['enforceHealthCheck'] === undefined && (value as Record<string, any>)['enforce_health_check'] === undefined)) return false;
    if ((!('healthStatus' in (value as Record<string, any>)) && !('health_status' in (value as Record<string, any>))) || ((value as Record<string, any>)['healthStatus'] === undefined && (value as Record<string, any>)['health_status'] === undefined)) return false;
    if (!('id' in value) || value['id'] === undefined) return false;
    if ((!('modelProvider' in (value as Record<string, any>)) && !('model_provider' in (value as Record<string, any>))) || ((value as Record<string, any>)['modelProvider'] === undefined && (value as Record<string, any>)['model_provider'] === undefined)) return false;
    if ((!('modelSyncMode' in (value as Record<string, any>)) && !('model_sync_mode' in (value as Record<string, any>))) || ((value as Record<string, any>)['modelSyncMode'] === undefined && (value as Record<string, any>)['model_sync_mode'] === undefined)) return false;
    if (!('name' in value) || value['name'] === undefined) return false;
    if ((!('orgId' in (value as Record<string, any>)) && !('org_id' in (value as Record<string, any>))) || ((value as Record<string, any>)['orgId'] === undefined && (value as Record<string, any>)['org_id'] === undefined)) return false;
    if ((!('secretPath' in (value as Record<string, any>)) && !('secret_path' in (value as Record<string, any>))) || ((value as Record<string, any>)['secretPath'] === undefined && (value as Record<string, any>)['secret_path'] === undefined)) return false;
    if (!('settings' in value) || value['settings'] === undefined) return false;
    if ((!('updatedAt' in (value as Record<string, any>)) && !('updated_at' in (value as Record<string, any>))) || ((value as Record<string, any>)['updatedAt'] === undefined && (value as Record<string, any>)['updated_at'] === undefined)) return false;
    return true;
}

export function ProviderFromJSON(json: any): Provider {
    return ProviderFromJSONTyped(json, false);
}

export function ProviderFromJSONTyped(json: any, ignoreDiscriminator: boolean): Provider {
    if (json == null) {
        return json;
    }
    return {
        
        'createdByEmail': json['created_by_email'] === undefined ? undefined : json['created_by_email'] === null ? null : json['created_by_email'],
        'createdByName': json['created_by_name'] === undefined ? undefined : json['created_by_name'] === null ? null : json['created_by_name'],
        'createdByUserId': json['created_by_user_id'] === undefined ? undefined : json['created_by_user_id'] === null ? null : json['created_by_user_id'],
        'lastChangeNote': json['last_change_note'] === undefined ? undefined : json['last_change_note'] === null ? null : json['last_change_note'],
        'lastModifiedAt': json['last_modified_at'] === undefined ? undefined : json['last_modified_at'] === null ? null : (parseDateTime(json['last_modified_at'])),
        'lastModifiedByEmail': json['last_modified_by_email'] === undefined ? undefined : json['last_modified_by_email'] === null ? null : json['last_modified_by_email'],
        'lastModifiedByName': json['last_modified_by_name'] === undefined ? undefined : json['last_modified_by_name'] === null ? null : json['last_modified_by_name'],
        'lastModifiedByUserId': json['last_modified_by_user_id'] === undefined ? undefined : json['last_modified_by_user_id'] === null ? null : json['last_modified_by_user_id'],
        'authType': json['auth_type'],
        'baseUrl': json['base_url'],
        'billingMode': BillingModeFromJSON(json['billing_mode']),
        'billingNote': json['billing_note'] === undefined ? undefined : json['billing_note'] === null ? null : json['billing_note'],
        'billingReason': json['billing_reason'] === undefined ? undefined : json['billing_reason'] === null ? null : BillingReasonFromJSON(json['billing_reason']),
        'catalogId': json['catalog_id'] === undefined ? undefined : json['catalog_id'] === null ? null : json['catalog_id'],
        'catalogSlug': json['catalog_slug'] === undefined ? undefined : json['catalog_slug'] === null ? null : json['catalog_slug'],
        'connectionId': json['connection_id'] === undefined ? undefined : json['connection_id'] === null ? null : json['connection_id'],
        'createdAt': (json['created_at'] == null ? json['created_at'] : parseDateTime(json['created_at'])),
        'enabled': json['enabled'],
        'enforceHealthCheck': json['enforce_health_check'],
        'healthCheckedAt': json['health_checked_at'] === undefined ? undefined : json['health_checked_at'] === null ? null : (parseDateTime(json['health_checked_at'])),
        'healthDetail': json['health_detail'] === undefined ? undefined : json['health_detail'] === null ? null : json['health_detail'],
        'healthStatus': json['health_status'],
        'id': json['id'],
        'modelProvider': json['model_provider'],
        'modelSyncMode': ModelSyncModeFromJSON(json['model_sync_mode']),
        'name': json['name'],
        'orgId': json['org_id'],
        'requestTimeoutSecs': json['request_timeout_secs'] === undefined ? undefined : json['request_timeout_secs'] === null ? null : json['request_timeout_secs'],
        'secretPath': json['secret_path'],
        'settings': json['settings'],
        'streamIdleTimeoutSecs': json['stream_idle_timeout_secs'] === undefined ? undefined : json['stream_idle_timeout_secs'] === null ? null : json['stream_idle_timeout_secs'],
        'updatedAt': (json['updated_at'] == null ? json['updated_at'] : parseDateTime(json['updated_at'])),
    };
}

export function ProviderToJSON(json: any): Provider {
    return ProviderToJSONTyped(json, false);
}

export function ProviderToJSONTyped(value?: Provider | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'created_by_email': value['createdByEmail'],
        'created_by_name': value['createdByName'],
        'created_by_user_id': value['createdByUserId'],
        'last_change_note': value['lastChangeNote'],
        'last_modified_at': value['lastModifiedAt'] == null ? value['lastModifiedAt'] : serializeDateTime(value['lastModifiedAt']),
        'last_modified_by_email': value['lastModifiedByEmail'],
        'last_modified_by_name': value['lastModifiedByName'],
        'last_modified_by_user_id': value['lastModifiedByUserId'],
        'auth_type': value['authType'],
        'base_url': value['baseUrl'],
        'billing_mode': BillingModeToJSON(value['billingMode']),
        'billing_note': value['billingNote'],
        'billing_reason': BillingReasonToJSON(value['billingReason']),
        'catalog_id': value['catalogId'],
        'catalog_slug': value['catalogSlug'],
        'connection_id': value['connectionId'],
        'created_at': value['createdAt'] == null ? value['createdAt'] : serializeDateTime(value['createdAt']),
        'enabled': value['enabled'],
        'enforce_health_check': value['enforceHealthCheck'],
        'health_checked_at': value['healthCheckedAt'] == null ? value['healthCheckedAt'] : serializeDateTime(value['healthCheckedAt']),
        'health_detail': value['healthDetail'],
        'health_status': value['healthStatus'],
        'id': value['id'],
        'model_provider': value['modelProvider'],
        'model_sync_mode': ModelSyncModeToJSON(value['modelSyncMode']),
        'name': value['name'],
        'org_id': value['orgId'],
        'request_timeout_secs': value['requestTimeoutSecs'],
        'secret_path': value['secretPath'],
        'settings': value['settings'],
        'stream_idle_timeout_secs': value['streamIdleTimeoutSecs'],
        'updated_at': value['updatedAt'] == null ? value['updatedAt'] : serializeDateTime(value['updatedAt']),
    };
}

