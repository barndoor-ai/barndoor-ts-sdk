/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


/**
 * What kind of destination a channel delivers to. The type is the discriminator for the
 * whole channel model: it decides which destination field is required, whether the channel is
 * personal (owned by one user) or org-wide (managed by an admin), and which natural-identity
 * key an upsert without ``id`` dedupes on.
 * 
 * Personal — owner is always the authenticated caller, never taken from the request body:
 * 
 * - ``in_app``: the caller's notification inbox in the platform UI. No destination field.
 * - ``user_email``: the caller's own verified account email. No destination field.
 * 
 * Org-wide — admin/superadmin managed, one set per organization:
 * 
 * - ``email``: an arbitrary address (a team alias, a ticketing inbox). Requires
 *   ``email_address``; deduped on it.
 * - ``webhook``: an HTTPS endpoint you own, signed with a Standard Webhooks
 *   ``whsec_`` secret. Requires ``url``; deduped on it.
 * - ``slack``: a channel in the organization's connected Slack workspace. Requires
 *   ``slack_channel_id`` + ``label``; needs the Slack app installed first (an interactive
 *   consent flow that is not part of this API).
 * - ``teams``: a Microsoft Teams channel behind a Workflows incoming-webhook URL. Requires
 *   ``label``, plus ``teams_workflow_url`` on create.
 * @export
 */
export const ChannelType = {
    InApp: 'in_app',
    UserEmail: 'user_email',
    Email: 'email',
    Webhook: 'webhook',
    Slack: 'slack',
    Teams: 'teams',
} as const;
export type ChannelType = typeof ChannelType[keyof typeof ChannelType];


export function instanceOfChannelType(value: any): boolean {
    for (const key in ChannelType) {
        if (Object.prototype.hasOwnProperty.call(ChannelType, key)) {
            if (ChannelType[key as keyof typeof ChannelType] === value) {
                return true;
            }
        }
    }
    return false;
}

export function ChannelTypeFromJSON(json: any): ChannelType {
    return ChannelTypeFromJSONTyped(json, false);
}

export function ChannelTypeFromJSONTyped(json: any, ignoreDiscriminator: boolean): ChannelType {
    return json as ChannelType;
}

export function ChannelTypeToJSON(value?: ChannelType | null): any {
    return value as any;
}

export function ChannelTypeToJSONTyped(value: any, ignoreDiscriminator: boolean): ChannelType {
    return value as ChannelType;
}

