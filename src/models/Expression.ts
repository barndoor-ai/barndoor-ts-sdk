/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
/**
 * 
 * @export
 * @interface Expression
 */
export interface Expression {
    /**
     * A single CEL expression that must evaluate to a boolean. Top-level variables: C, G, P, R, V, constants, globals, request, runtime, variables. `P` is the calling principal and `R` the resource (the tool call being authorized). Commonly used attributes: `P.attr.groups` and `P.attr.roles` hold the UNPREFIXED role and group names from the user's profile — note the contrast with a rule's `roles_groups`, whose entries are `role:`/`group:`-prefixed; other IdP profile fields such as `P.attr.department` are available; and `R.attr.<argument>` exposes the tool call's own arguments. Expressions are compiled at write time and rejected if invalid or if they reference an unknown variable.
     */
    expr: string;
}

/**
 * Check if a given object implements the Expression interface.
 */
export function instanceOfExpression(value: object): value is Expression {
    if (!('expr' in value) || value['expr'] === undefined) return false;
    return true;
}

export function ExpressionFromJSON(json: any): Expression {
    return ExpressionFromJSONTyped(json, false);
}

export function ExpressionFromJSONTyped(json: any, ignoreDiscriminator: boolean): Expression {
    if (json == null) {
        return json;
    }
    return {
        
        'expr': json['expr'],
    };
}

export function ExpressionToJSON(json: any): Expression {
    return ExpressionToJSONTyped(json, false);
}

export function ExpressionToJSONTyped(value?: Expression | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'expr': value['expr'],
    };
}

