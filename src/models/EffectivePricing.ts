/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime.js';
import type { LongContextTier } from './LongContextTier.js';
import {
    LongContextTierFromJSON,
    LongContextTierFromJSONTyped,
    LongContextTierToJSON,
    LongContextTierToJSONTyped,
} from './LongContextTier.js';

/**
 * The rate a request against a given mapping would be billed at, plus which
 * tier produced it.
 * 
 * `source` is always present. Every other field is `None` when
 * `source == "none"` — meaning no rule and no Barndoor default covers this
 * model, so requests succeed but record $0. The rates are deliberately
 * nullable rather than zero-filled: a UI that renders `$0.00` for an
 * unpriced model reads as "this model is free", which is exactly the wrong
 * conclusion.
 * 
 * Clients distinguish four states:
 * 
 * * field absent entirely → backend predates this annotation
 * * `source == "none"` → unpriced, bills at $0 because the rate is *unknown*
 * * `source == "not_metered"` → bills at $0 *by decision* (BCP-3876): the
 *   provider's `billing_mode` is `subscription`, so no rate is consulted.
 *   Rates are `None` here too, but for the opposite reason, and the UI must
 *   not lump it in with "unpriced" — that bucket is a misconfiguration it
 *   nags about and offers to bulk-disable.
 * * anything else → priced, `input`/`output` populated
 * @export
 * @interface EffectivePricing
 */
export interface EffectivePricing {
    /**
     * Prompt-cache read rate. `None` on a *priced* row means the rule sets
     * no explicit cache rate, and the calculator bills cached input at the
     * base input rate — render it as an inherited default, never as free.
     */
    cacheReadCostPerMillionTokens?: number | null;
    /**
     * Prompt-cache write rate. Same `None` semantics as the read rate.
     */
    cacheWriteCostPerMillionTokens?: number | null;
    /**
     * 
     */
    inputCostPerMillionTokens?: number | null;
    /**
     * The long-context surcharge on the matched rule (BCP-3904), when it
     * has one.
     * 
     * Present here for the same reason the rates are: an admin ordering
     * route priorities on cost has to be able to see that a target's price
     * doubles above a prompt-size threshold. Rendering only the
     * short-context rate for a Grok or GPT-6 route would understate its
     * cost on exactly the long-context traffic that makes the choice
     * matter.
     */
    longContext?: LongContextTier | null;
    /**
     * 
     */
    outputCostPerMillionTokens?: number | null;
    /**
     * The `model_pricing` version row that matched, so the UI can link a
     * displayed rate back to the rule that produced it. `None` when
     * `source == "none"`.
     */
    pricingRuleId?: string | null;
    /**
     * Which resolution tier matched: `org_instance`, `org_catalog`,
     * `org_generic`, `barndoor_default`, or `none`. Mirrors
     * [`PricingSource::as_str`] so the value matches what audit events
     * already record for the same decision.
     */
    source: string;
}

/**
 * Check if a given object implements the EffectivePricing interface.
 */
export function instanceOfEffectivePricing(value: object): value is EffectivePricing {
    if (!('source' in value) || value['source'] === undefined) return false;
    return true;
}

export function EffectivePricingFromJSON(json: any): EffectivePricing {
    return EffectivePricingFromJSONTyped(json, false);
}

export function EffectivePricingFromJSONTyped(json: any, ignoreDiscriminator: boolean): EffectivePricing {
    if (json == null) {
        return json;
    }
    return {
        
        'cacheReadCostPerMillionTokens': json['cache_read_cost_per_million_tokens'] === undefined ? undefined : json['cache_read_cost_per_million_tokens'] === null ? null : json['cache_read_cost_per_million_tokens'],
        'cacheWriteCostPerMillionTokens': json['cache_write_cost_per_million_tokens'] === undefined ? undefined : json['cache_write_cost_per_million_tokens'] === null ? null : json['cache_write_cost_per_million_tokens'],
        'inputCostPerMillionTokens': json['input_cost_per_million_tokens'] === undefined ? undefined : json['input_cost_per_million_tokens'] === null ? null : json['input_cost_per_million_tokens'],
        'longContext': json['long_context'] === undefined ? undefined : json['long_context'] === null ? null : LongContextTierFromJSON(json['long_context']),
        'outputCostPerMillionTokens': json['output_cost_per_million_tokens'] === undefined ? undefined : json['output_cost_per_million_tokens'] === null ? null : json['output_cost_per_million_tokens'],
        'pricingRuleId': json['pricing_rule_id'] === undefined ? undefined : json['pricing_rule_id'] === null ? null : json['pricing_rule_id'],
        'source': json['source'],
    };
}

export function EffectivePricingToJSON(json: any): EffectivePricing {
    return EffectivePricingToJSONTyped(json, false);
}

export function EffectivePricingToJSONTyped(value?: EffectivePricing | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'cache_read_cost_per_million_tokens': value['cacheReadCostPerMillionTokens'],
        'cache_write_cost_per_million_tokens': value['cacheWriteCostPerMillionTokens'],
        'input_cost_per_million_tokens': value['inputCostPerMillionTokens'],
        'long_context': LongContextTierToJSON(value['longContext']),
        'output_cost_per_million_tokens': value['outputCostPerMillionTokens'],
        'pricing_rule_id': value['pricingRuleId'],
        'source': value['source'],
    };
}

