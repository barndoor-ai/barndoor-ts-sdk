/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues, parseDate, parseDateTime, serializeDate, serializeDateTime } from '../runtime.js';
import type { PricingChangeSource } from './PricingChangeSource.js';
import {
    PricingChangeSourceFromJSON,
    PricingChangeSourceFromJSONTyped,
    PricingChangeSourceToJSON,
    PricingChangeSourceToJSONTyped,
} from './PricingChangeSource.js';
import type { LongContextTier } from './LongContextTier.js';
import {
    LongContextTierFromJSON,
    LongContextTierFromJSONTyped,
    LongContextTierToJSON,
    LongContextTierToJSONTyped,
} from './LongContextTier.js';
import type { PricingSyncMode } from './PricingSyncMode.js';
import {
    PricingSyncModeFromJSON,
    PricingSyncModeFromJSONTyped,
    PricingSyncModeToJSON,
    PricingSyncModeToJSONTyped,
} from './PricingSyncMode.js';

/**
 * One entry in the per-rule list returned by `GET /admin/model-pricing`.
 * `current` is the latest version with `effective_from <= now()` (so the
 * row UI can render exactly as it did pre-V34), `scheduled_count` /
 * `next_scheduled_effective_from` drive the small "Scheduled" affordance on
 * the row, and `version_count` decides whether to show the history icon.
 * @export
 * @interface PricingRuleSummary
 */
export interface PricingRuleSummary {
    /**
     * Per-million-token cost for input tokens served from the prompt
     * cache. `None` ⇒ the calculator bills cached input at
     * `input_cost_per_million_tokens`.
     */
    cacheReadCostPerMillionTokens?: number;
    /**
     * Per-million-token cost for input tokens added to the prompt cache.
     * `None` ⇒ same fallback as `cache_read_cost_per_million_tokens`.
     */
    cacheWriteCostPerMillionTokens?: number;
    /**
     * Catalog slug for provider-level pricing (e.g. "openai", "aws_bedrock").
     */
    catalogSlug?: string;
    /**
     * 
     */
    effectiveFrom: Date;
    /**
     * 
     */
    id: string;
    /**
     * 
     */
    inputCostPerMillionTokens: number;
    /**
     * Optional context-tiered surcharge (BCP-3904). `None` ⇒ one rate set
     * at every prompt size, i.e. exactly the pre-BCP-3904 behaviour.
     * 
     * Serialized as `long_context`, not as the Rust field name. This struct
     * reaches the admin API through `AdminModelPricing`'s
     * `#[serde(flatten)]`, so the field name here *is* the wire key — and
     * every sibling surface (`DefaultPricingEntry`, `EffectivePricing`, the
     * create/update request bodies) already spells it `long_context`. Left
     * unrenamed, the pricing tab would write a tier successfully and then
     * read back nothing, which is precisely how it behaved before this
     * attribute existed: the surcharge saved, the dialog reopened unchecked,
     * and the edit looked silently discarded. Pinned by
     * `admin_model_pricing_serializes_the_tier_as_long_context`.
     */
    longContext?: LongContextTier;
    /**
     * 
     */
    modelPattern: string;
    /**
     * 
     */
    modelProvider?: string;
    /**
     * 
     */
    orgId: string;
    /**
     * 
     */
    outputCostPerMillionTokens: number;
    /**
     * Optional reference to a specific LLM Gateway provider instance.
     */
    providerId?: string;
    /**
     * 
     */
    providerName?: string;
    /**
     * 
     */
    changeReason?: string;
    /**
     * 
     */
    changeSource: PricingChangeSource;
    /**
     * 
     */
    createdByEmail?: string;
    /**
     * 
     */
    createdByName?: string;
    /**
     * 
     */
    createdByUserId?: string;
    /**
     * True for tombstone rows produced by `archive_rule` (V35). When
     * the latest effective version of a group has `is_archived = true`,
     * the rule is considered archived: the cost calculator skips it
     * and the admin summary list hides it by default. Past non-archived
     * versions remain in the timeline so the History dialog can show
     * the rule's full life including the archive event.
     */
    isArchived: boolean;
    /**
     * 
     */
    syncMode: PricingSyncMode;
    /**
     * When the next scheduled version takes effect, if any.
     */
    nextScheduledEffectiveFrom?: Date | null;
    /**
     * Versions of this rule with `effective_from > now()` that haven't
     * activated yet. `0` means no scheduled change pending.
     */
    scheduledCount: number;
    /**
     * Total versions in this rule's history (including `current`).
     */
    versionCount: number;
}



/**
 * Check if a given object implements the PricingRuleSummary interface.
 */
export function instanceOfPricingRuleSummary(value: object): value is PricingRuleSummary {
    if ((!('effectiveFrom' in (value as Record<string, any>)) && !('effective_from' in (value as Record<string, any>))) || ((value as Record<string, any>)['effectiveFrom'] === undefined && (value as Record<string, any>)['effective_from'] === undefined)) return false;
    if (!('id' in value) || value['id'] === undefined) return false;
    if ((!('inputCostPerMillionTokens' in (value as Record<string, any>)) && !('input_cost_per_million_tokens' in (value as Record<string, any>))) || ((value as Record<string, any>)['inputCostPerMillionTokens'] === undefined && (value as Record<string, any>)['input_cost_per_million_tokens'] === undefined)) return false;
    if ((!('modelPattern' in (value as Record<string, any>)) && !('model_pattern' in (value as Record<string, any>))) || ((value as Record<string, any>)['modelPattern'] === undefined && (value as Record<string, any>)['model_pattern'] === undefined)) return false;
    if ((!('orgId' in (value as Record<string, any>)) && !('org_id' in (value as Record<string, any>))) || ((value as Record<string, any>)['orgId'] === undefined && (value as Record<string, any>)['org_id'] === undefined)) return false;
    if ((!('outputCostPerMillionTokens' in (value as Record<string, any>)) && !('output_cost_per_million_tokens' in (value as Record<string, any>))) || ((value as Record<string, any>)['outputCostPerMillionTokens'] === undefined && (value as Record<string, any>)['output_cost_per_million_tokens'] === undefined)) return false;
    if ((!('changeSource' in (value as Record<string, any>)) && !('change_source' in (value as Record<string, any>))) || ((value as Record<string, any>)['changeSource'] === undefined && (value as Record<string, any>)['change_source'] === undefined)) return false;
    if ((!('isArchived' in (value as Record<string, any>)) && !('is_archived' in (value as Record<string, any>))) || ((value as Record<string, any>)['isArchived'] === undefined && (value as Record<string, any>)['is_archived'] === undefined)) return false;
    if ((!('syncMode' in (value as Record<string, any>)) && !('sync_mode' in (value as Record<string, any>))) || ((value as Record<string, any>)['syncMode'] === undefined && (value as Record<string, any>)['sync_mode'] === undefined)) return false;
    if ((!('scheduledCount' in (value as Record<string, any>)) && !('scheduled_count' in (value as Record<string, any>))) || ((value as Record<string, any>)['scheduledCount'] === undefined && (value as Record<string, any>)['scheduled_count'] === undefined)) return false;
    if ((!('versionCount' in (value as Record<string, any>)) && !('version_count' in (value as Record<string, any>))) || ((value as Record<string, any>)['versionCount'] === undefined && (value as Record<string, any>)['version_count'] === undefined)) return false;
    return true;
}

export function PricingRuleSummaryFromJSON(json: any): PricingRuleSummary {
    return PricingRuleSummaryFromJSONTyped(json, false);
}

export function PricingRuleSummaryFromJSONTyped(json: any, ignoreDiscriminator: boolean): PricingRuleSummary {
    if (json == null) {
        return json;
    }
    return {
        
        'cacheReadCostPerMillionTokens': json['cache_read_cost_per_million_tokens'] == null ? undefined : json['cache_read_cost_per_million_tokens'],
        'cacheWriteCostPerMillionTokens': json['cache_write_cost_per_million_tokens'] == null ? undefined : json['cache_write_cost_per_million_tokens'],
        'catalogSlug': json['catalog_slug'] == null ? undefined : json['catalog_slug'],
        'effectiveFrom': (json['effective_from'] == null ? json['effective_from'] : parseDateTime(json['effective_from'])),
        'id': json['id'],
        'inputCostPerMillionTokens': json['input_cost_per_million_tokens'],
        'longContext': json['long_context'] == null ? undefined : LongContextTierFromJSON(json['long_context']),
        'modelPattern': json['model_pattern'],
        'modelProvider': json['model_provider'] == null ? undefined : json['model_provider'],
        'orgId': json['org_id'],
        'outputCostPerMillionTokens': json['output_cost_per_million_tokens'],
        'providerId': json['provider_id'] == null ? undefined : json['provider_id'],
        'providerName': json['provider_name'] == null ? undefined : json['provider_name'],
        'changeReason': json['change_reason'] == null ? undefined : json['change_reason'],
        'changeSource': PricingChangeSourceFromJSON(json['change_source']),
        'createdByEmail': json['created_by_email'] == null ? undefined : json['created_by_email'],
        'createdByName': json['created_by_name'] == null ? undefined : json['created_by_name'],
        'createdByUserId': json['created_by_user_id'] == null ? undefined : json['created_by_user_id'],
        'isArchived': json['is_archived'],
        'syncMode': PricingSyncModeFromJSON(json['sync_mode']),
        'nextScheduledEffectiveFrom': json['next_scheduled_effective_from'] === undefined ? undefined : json['next_scheduled_effective_from'] === null ? null : (parseDateTime(json['next_scheduled_effective_from'])),
        'scheduledCount': json['scheduled_count'],
        'versionCount': json['version_count'],
    };
}

export function PricingRuleSummaryToJSON(json: any): PricingRuleSummary {
    return PricingRuleSummaryToJSONTyped(json, false);
}

export function PricingRuleSummaryToJSONTyped(value?: PricingRuleSummary | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'cache_read_cost_per_million_tokens': value['cacheReadCostPerMillionTokens'],
        'cache_write_cost_per_million_tokens': value['cacheWriteCostPerMillionTokens'],
        'catalog_slug': value['catalogSlug'],
        'effective_from': value['effectiveFrom'] == null ? value['effectiveFrom'] : serializeDateTime(value['effectiveFrom']),
        'id': value['id'],
        'input_cost_per_million_tokens': value['inputCostPerMillionTokens'],
        'long_context': LongContextTierToJSON(value['longContext']),
        'model_pattern': value['modelPattern'],
        'model_provider': value['modelProvider'],
        'org_id': value['orgId'],
        'output_cost_per_million_tokens': value['outputCostPerMillionTokens'],
        'provider_id': value['providerId'],
        'provider_name': value['providerName'],
        'change_reason': value['changeReason'],
        'change_source': PricingChangeSourceToJSON(value['changeSource']),
        'created_by_email': value['createdByEmail'],
        'created_by_name': value['createdByName'],
        'created_by_user_id': value['createdByUserId'],
        'is_archived': value['isArchived'],
        'sync_mode': PricingSyncModeToJSON(value['syncMode']),
        'next_scheduled_effective_from': value['nextScheduledEffectiveFrom'] == null ? value['nextScheduledEffectiveFrom'] : serializeDateTime(value['nextScheduledEffectiveFrom']),
        'scheduled_count': value['scheduledCount'],
        'version_count': value['versionCount'],
    };
}

