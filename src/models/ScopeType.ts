/* tslint:disable */
/* eslint-disable */
/**
 * Barndoor.ai Public API
 * Unified public API specification for Barndoor.ai platform
 *
 * The version of the OpenAPI document: 2.2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


/**
 * Scope is the *identity* axis of a budget or rate-limit policy — "who is
 * this policy for?". The *what* (LLM provider, upstream model name, model
 * alias) lives on dedicated `target_*` columns on the row (BCP-2813), not
 * as a scope value. `LlmProvider` and `Model` remain in the enum because
 * `rate_limit_policies` and `model_access_policies` still accept them in
 * their `scope_type` CHECK constraints (V12 / V18 / V22 widenings — a
 * separate scope of work to clean up). For token budgets the CHECK was
 * tightened back to identity-only by `V41__budget_scope_target.sql`, so
 * any `LlmProvider`/`Model` budget row that survived would simply fail to
 * match in `BudgetEnforcer::matched_keys`.
 * @export
 */
export const ScopeType = {
    Org: 'org',
    Team: 'team',
    User: 'user',
    Project: 'project',
    ApiKey: 'api_key',
    McpServer: 'mcp_server',
    LlmProvider: 'llm_provider',
    Model: 'model',
    Agent: 'agent',
    Role: 'role',
    Group: 'group',
} as const;
export type ScopeType = typeof ScopeType[keyof typeof ScopeType];


export function instanceOfScopeType(value: any): boolean {
    for (const key in ScopeType) {
        if (Object.prototype.hasOwnProperty.call(ScopeType, key)) {
            if (ScopeType[key as keyof typeof ScopeType] === value) {
                return true;
            }
        }
    }
    return false;
}

export function ScopeTypeFromJSON(json: any): ScopeType {
    return ScopeTypeFromJSONTyped(json, false);
}

export function ScopeTypeFromJSONTyped(json: any, ignoreDiscriminator: boolean): ScopeType {
    return json as ScopeType;
}

export function ScopeTypeToJSON(value?: ScopeType | null): any {
    return value as any;
}

export function ScopeTypeToJSONTyped(value: any, ignoreDiscriminator: boolean): ScopeType {
    return value as ScopeType;
}

